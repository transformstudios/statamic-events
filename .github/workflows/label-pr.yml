name: PR Labeler

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  label:
    uses: edalzell/github-workflows/.github/workflows/label-pr.yml@5ebfc6714a6c4ca7f2434ff01736ea2405e1c323 # v1.3.1
    permissions:
      contents: read
      pull-requests: write
